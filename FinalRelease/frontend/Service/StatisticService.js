import * as url from '../Constant/Url';
import {postRequest} from '../Util/Ajax';

//获取作业最高分，最低分，平均分
// export const getScore = (courseID, homeworkName, callback) => {
//     var data = {courseID: courseID, account: homeworkName};
//     postRequest(url.HOMEWORK_URL + 'statistic', data, callback);
// };
